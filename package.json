{
  "main": "index.js",  // Specifies the main entry point of your application
  "type": "module",    // Set to module if you're using ES modules (import/export)
  "dependencies": {
    "ejs": "^3.1.10",
    "express": "^4.21.1",
    "method-override": "^3.0.0",
    "mongoose": "^8.8.0",
    "mongosh": "^2.3.3",
    "nodemon": "^3.1.7"
  },
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error : no test specified\" && exit 1"
  }
}
